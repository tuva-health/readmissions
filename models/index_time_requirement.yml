
version: 2

models:
  - name: index_time_requirement
    config:
      database: "{{ var('readmissions_output_database', var('output_database')) }}"
      schema: "{{ var('readmissions_output_schema', var('output_schema')) }}"
    description: "This model lists all encounters that meet the time requirement to be an index admission."
    columns:
      - name: encounter_id
        description: "The unique identifier for the encounter"
