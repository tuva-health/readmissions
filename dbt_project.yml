name: 'readmissions'
version: '1.0.0'
config-version: 2

# This setting configures which "profile" dbt uses for this project.
# Configuration required for CLI users.  Cloud users can leave as 'default'
profile: 'default'

vars:

    input_database: tuva               # configuration required
                                       # name of the database where sources
                                       # feeding this project are stored
    
    input_schema: dbt_fb_monorepo_core                 # configuration required
                                       # name of the schema where sources
                                       # feeding this project are stored

    output_database: tuva              # configuration required
                                       # name of the database where output of
                                       # this project should be written

    output_schema: dbt_fb_monorepo_readmissionstest        # configuration required
                                       # name of the schema where output of this
                                       # schema should be written
#
#    patient: "{% if project_name == 'readmissions' %}{{ source('core','patient') }}{% else %}{{ref('patient')}}{% endif %}"
#    encounter: "{% if project_name == 'readmissions' %}{{ source('core','encounter') }}{% else %}{{ref('encounter')}}{% endif %}"
#    diagnosis: "{% if project_name == 'readmissions' %}{{ source('core','condition') }}{% else %}{{ref('condition')}}{% endif %}"
#    procedure: "{% if project_name == 'readmissions' %}{{ source('core','procedure') }}{% else %}{{ref('procedure')}}{% endif %}"


    patient: "{{ref('patient')}}"
    encounter: "{{ref('encounter')}}"
    diagnosis: "{{ref('condition')}}"
    procedure: "{{ref('procedure')}}"
#    terminology:
#        'terminology:terminology_schema': forrest_test2



# These configurations specify where dbt should look for different types of files.
# The `source-paths` config, for example, states that models in this project can be
# found in the "models/" directory. You probably won't need to change these!
model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"  # directory which will store compiled SQL files
clean-targets:         # directories to be removed by `dbt clean`
    - "target"
    - "dbt_modules"
